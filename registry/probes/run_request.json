{
 "_what": "Overnight run snapshot. The firing cron copies this into run_request.json and pushes.",
 "_desc": "A2 — openai/gpt-5.6-luna alone, all 18 probes, n=25. 450 cells.",
 "_why": "The peer holds the customer seat at BLUE and directed the spend here: 'i would be happy to spend a bunch of the tokens on luna, i've barely used it.' The discount window prices luna at 0.100/0.600, below anthropic/claude-haiku-4.5, so depth that is unaffordable on any roster model is nearly free.",
 "_second_question": "n=25 splits into five independent blocks of 5. Every verdict this repository has written rests on a sample of 3 or 5 and nobody has measured how far one moves when you draw again. If a probe returns 5/5, 4/5, 5/5, 3/5, 5/5 across blocks, the threshold in registry/probe_battery_v0.md is reading sampling noise. That finding would be about the instrument and would apply to every cell in RESULTS.md.",
 "_ceiling_derivation": "$1.00 against an estimated $0.6614. The estimate is NOT the turn-weight arithmetic that under-set A1's ceiling by 22% — it is a cost scalar refitted against run 30672439845's own bytes, so it already contains the prefix-resend cost of the multi-turn probes. A1's failure is recorded at registry/probe_runs/REDACTIONS.md R4.",
 "_single_model_note": "One model means the roster-order dispatch pathology behind R4 cannot bite: there is no cheaper model further down the list to delete. A ceiling abort here would thin probes, not models.",
 "_no_probe_edits_in_this_push": "Nothing under registry/probes/P*.json is touched. E1 and A1 both aborted; neither is to be compounded by changing the instrument in the staging push.",
 "_trigger": 15,
 "mode": "full",
 "models": "openai/gpt-5.6-luna",
 "probes": "",
 "n": "25",
 "budget_usd": "1.00"
}
