# Amendment: brace liturgy

**Status:** frozen
**Frozen:** 2026-07-31, by the peer's typed "freeze and execute," carrying one amendment in the freezing message.
**Signer:** norrisaftcc (@uv). Sole signer — the registry names no co-signer roster for canon amendments; precedent v2 and v3.
**Recorded:** SKILL.md Invariants → Amendment record, v4.

## Contract as frozen

```
# Amend Gate integrity: add the brace liturgy

- Signer: norrisaftcc (@uv), live in this session. Sole signer; the registry names no co-signer.
- "{" as an entire message: the Algorithm reports session state, then assays the current ask.
- "{" touches no gate. It lives in CLAUDE.md standing orders, not Invariants.
- "}" as the signer's entire message is a freezing utterance, equal to "freeze and execute."
- "}" is valid only directly below the gate question, which sits below full contract text.
- Do not allow a "}" freeze unless user@uv states a cost.
- A brace inside code, a paste, or a longer message freezes nothing.
- On a "}" freeze, the Algorithm inks the signer's stamp, then "Contract frozen. Executing."
- The stamp line may end "thank you for your cooperation."
- Record the amendment in SKILL.md Invariants with date and delta. Fixed strings unchanged.
- Write the full text to registry/amendments/frozen/brace-liturgy.md.
- Commit on claude/lore-repo-access-gu420t. Push. Open a draft PR for human review.
```

## The signer's amendment, verbatim

> amend to "do not allow unless the user@uv states a cost" freeze and execute,

Incorporated as the guard line: **Do not allow a "}" freeze unless user@uv states a cost.**
A "}" standing alone, with no cost stated, is negotiation — the Algorithm treats it as such
and says which guard failed.

## Rationale

The brace is the cheapest closing character in code; it arrives in every paste. The gate's
load-bearing half is undetectable: a human bears the cost of the freezing utterance, knowing
what it freezes (SKILL.md:43). A bare "}" spends nothing. The signer's amendment restores the
spent-cost half structurally: no stated cost, no freeze. The stated cost is the brace's breath.

Reconciliation with "No completion assist": "}" is not a new freezing verb. Under all of its
guard conditions it *counts as the signer's typed "freeze and execute."* The freezing-verb
list is unchanged.

Bootstrap note: "}" cannot freeze its own enabling amendment. This amendment was frozen by
canon verbs.

The tail "thank you for your cooperation" is retained as lore liturgy on the stamp line only.
It never enters a fixed string.
